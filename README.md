# climb
C LIbrary for Modern Backends (CLIMB)

Bored being compelled to program your backends with JavaScript ?

Try this good old still alive (and very fast) C then

With this public domain library, you can program a backend or an API with only few lines of C code
